name: Build Latest
on:
  push:
    branches:
      - main

jobs:
  install_deps:
    uses: bresnow/remix.gun/.github/workflows/node-dependencies.yml@main
  build_latest:
    needs: install_deps
    if: ${{ success() }}
    runs-on:
      - self-hosted
      - main-server
    steps:
      - uses: actions/checkout@v3
      - name: Build and Push As Latest
        run: |
          ts-node-esm scripts/docker/build-push-dh.ts -L
          ts-node-esm scripts/docker/build-push-dh.ts -L -G
          ts-node-esm scripts/docker/build-push-dh.ts
          ts-node-esm scripts/docker/build-push-dh.ts -G
