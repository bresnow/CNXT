name: Main Branch Workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:
    branches:
      - main
env:
  STACK_NAME: dev-cnxt_temp
  IMAGE: cnxt
  VERSION: 0.3.1
jobs:
  docker:
    runs-on:
      - self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image & Push to Github Registry
        run: zx ./scripts/docker/build-push-gh.mjs --image ${{env.IMAGE}} --version
          ${{env.VERSION}}
      - name: Build Docker Image & Push to Docker Registry
        run: zx ./scripts/docker/build-push-dh.mjs --image ${{env.IMAGE}} --version
          ${{env.VERSION}}
      - name: Update Swarm Stack
        run: zx ./scripts/docker/update-stack.mjs --image ${{env.IMAGE}} --version
          ${{env.VERSION}} --stack-name ${{env.STACK_NAME}}
