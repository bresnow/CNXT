name: Dependency Install
on:
  workflow_call:

jobs:
  build_latest:
    runs-on:
      - self-hosted
      - main-server
    steps:
      - uses: actions/checkout@v3
      - name: Node Dependencies
        run: |
          node -v
          yarn install
          yarn add global zx
          yarn add global typescript
